apiVersion: v1
kind: Service
metadata:
  name: babu-app-service
  namespace: default
spec:
  selector:
    app: babu-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
